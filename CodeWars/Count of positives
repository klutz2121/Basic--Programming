function countPositivesSumNegatives(num) {
var count=0,b=0;
if(num == null||num.length == 0){
  return [];
}
for(var i=0;i<num.length;i++)
{
  if(num[i]>0)
  {
    count++;
  }
  else if(num[i]<0)
  {
    b= b+num[i];
  }
}
    return [count,b];
} 
